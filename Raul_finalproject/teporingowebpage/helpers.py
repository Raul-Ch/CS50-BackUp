import csv
import datetime
#This is a Python library that allows accurate and cross-platform timezone calculations. It's often used when you need to deal with time data that's timezone-aware.
import pytz
# This is a popular Python library for making HTTP requests. It abstracts the complexities of making requests behind a beautiful, simple API so that you can focus on interacting with services and consuming data in your application.
import requests
# This is a Python module for fetching URLs (Uniform Resource Locators). It offers a very simple interface for doing complex tasks like opening, reading, and closing URLs.
import urllib

from flask import redirect, render_template,
from functools import wraps
