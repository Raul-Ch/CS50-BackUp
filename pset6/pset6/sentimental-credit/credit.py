# TODO
from cs50 import get_int

def checksum(credit):
    multiply = False
    check
    sum = 0
    length = 0

    while(credit > 0):
        if (multiply == True):
            check = credit % 10;
        elif:


credit_number = get_int("Input the credit card number: ")
checksum(credit_number)


